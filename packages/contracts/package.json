{
  "name": "@saas/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-domain TypeScript types — the wire format between bounded contexts. Types only; zero runtime dependencies (D1, D2).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run --typecheck --passWithNoTests"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "3.2.4"
  }
}
