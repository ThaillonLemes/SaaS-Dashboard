{
  "name": "@saas/normalization",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical entity model + ErpMapper contract — translates raw ERP payloads into tenant-scoped Product, Customer, and Order entities (Phase 1D Block 031).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@saas/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "typescript": "5.9.3",
    "vitest": "3.2.4"
  }
}
