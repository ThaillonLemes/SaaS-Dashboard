# Workspace State

_Source of truth for cross-domain current state._
_Replace-only. Updated by the Governor on every block integration._
_Last updated: (initial template — not yet populated)_

The Governor maintains this file by reading
`STATE.proposed.md` (regenerated by `governor state`) and editing the
table below by hand. Manifest agents do NOT edit this file.

---

## Active package work

| Package | Active agent | Current block | Worktree | Status |
|---------|--------------|---------------|----------|--------|
| _none yet_ | — | — | — | — |

Once the project starts:

```markdown
| identity      | Agent-A | block-007-mfa            | .claude/worktrees/identity-mfa/        | InProgress |
| analytics     | Agent-B | block-019-revenue-kpi    | .claude/worktrees/analytics-revenue/   | InProgress |
| dashboard     | (none)  | —                        | —                                       | —          |
```

---

## Active phase

**Phase:** _none yet — Phase 0 not started_

When a phase is active:

```markdown
**Phase:** Phase 0 — Foundation (sequential)
**Started:** 2026-05-15
**Blocks complete:** 003 / 010
**Next block:** block-004 (packages/contracts skeleton)
**Exit criteria:** see `phases/phase-0/exit.md`
```

---

## Package status

| Package | Status | Last block | Notes |
|---------|--------|------------|-------|
| `contracts` | not bootstrapped | — | — |
| `identity` | not bootstrapped | — | — |
| `tenancy` | not bootstrapped | — | — |
| `integrations` | not bootstrapped | — | — |
| `normalization` | not bootstrapped | — | — |
| `analytics` | not bootstrapped | — | — |
| `dashboard` | not bootstrapped | — | — |
| `ui-kit` | not bootstrapped | — | — |
| `observability` | not bootstrapped | — | — |

---

## Apps status

| App | Status | Notes |
|-----|--------|-------|
| `apps/api` | not bootstrapped | — |
| `apps/web` | not bootstrapped | — |

---

## Cross-cutting infrastructure

| Item | Status | Notes |
|------|--------|-------|
| Monorepo tooling (pnpm + turborepo) | not bootstrapped | Phase 0 Block 001 |
| Database (PostgreSQL) | not provisioned | Phase 0 Block 002 |
| CI/CD (GitHub Actions) | not configured | Phase 0 Block 009 |
| Deployment target | undecided | TBD at Phase 0 decisions |
| Orchestrator | installed (this package) | `orchestrator/` + `orchestrator.config.yaml` |

---

## Active features (cross-domain)

See `features.md`.

---

## Recent Governor activity

| Date | Action |
|------|--------|
| _initial_ | V3 foundation package installed |

---

## Worktree map (Governor-maintained)

Output of `governor scan` lives at `.governor/orchestrator/.cache.json`.
The Governor consults that file to compose the table above. Each row
corresponds to one active Claude Code worktree, one branch, and one
manifest.

---

## Format reference

After Phase 0 bootstrap completes, the Governor restructures this file
into operational form using the `governor state` output as input. The
`STATE.proposed.md` derived file is regenerated automatically on every
`governor scan`; it is never the source of truth — this file is.
